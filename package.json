{
  "name": "@ayd-ab/multer-azure-blob-storage",
  "version": "2.2.0",
  "description": "ES5/6 & Typescript friendly multer storage engine for Azure's blob storage.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "clean": "rm -rf dist",
    "prebuild": "npm run lint",
    "build": "tsc",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ayd-ab/multer-azure-blob-storage"
  },
  "keywords": [
    "multer",
    "azure",
    "express",
    "file upload",
    "azure storage",
    "blob",
    "typescript",
    "ES5",
    "ES6"
  ],
  "author": "Daniil Konoplev",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@types/multer": "^2.0.0",
    "@types/node": "^18.19.130",
    "tslint": "^6.1.3",
    "typescript": "^4.7.4"
  },
  "peerDependencies": {
    "express": ">=4.0.0",
    "multer": ">=2.0.0"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.29.1",
    "uuid": "^13.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
